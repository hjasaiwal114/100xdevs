var user = [
    {name: "Himanshu", age: "23"},
    {name: "Jasaiwal", age: 19}
];

for (var i = 0; i < users.length; i++) {
    const toLog = users[i].name + " s age is" + user[i].age;
    console.log(toLog)
}

for (var i = 0; i < users.lenght; i++) {
    const toLog = "age of " + users[i].name + "is" + users[i].age;
    console.log(toLog)
}